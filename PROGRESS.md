# Прогресс

Обновляется агентом после каждой завершённой задачи.
Читается в начале каждой сессии — здесь память проекта между запусками.

## Текущий спринт

**Спринт 4 — фронтенд. Статус: готов.**

## Сделано

### Спринт 1
- Каркас FastAPI, конфиг на pydantic-settings, ленивый async engine
- 9 моделей БД из раздела 6 ТЗ + начальная миграция `0001_initial`
- Шифрование `daily_logs.note` (AES-256-GCM)
- Скрабинг чувствительных полей для логов и Sentry
- `/health`, `/health/ready`
- docker-compose, Makefile, 12 тестов

### Спринт 2 — аутентификация
- argon2id, JWT HS256, ротация refresh-токенов с обнаружением кражи
- Валидация Telegram initData с проверкой auth_date < 24h
- 8 эндпоинтов /auth, 21 тест

### Спринт 3 — ядро
- Алгоритм прогноза по разделу 7 ТЗ (взвешенное среднее, отсечение
  выбросов, овуляция от конца цикла, регулярность по σ) — 22 юнит-теста
- 5 эндпоинтов /cycles с валидацией FR-1.5, 2 /predictions, 3 /logs
- 22 HTTP-теста. Итого 77 зелёных.

### Спринт 4 — фронтенд
- Vite + React 18 + TS + Tailwind + PWA plugin
- CSS-переменные из DESIGN-SPEC 2.1 (OKLCH, светлая и тёмная темы)
- i18n — свой лёгкий парсер ICU-плюралов (RU one/few/many/other), все строки
  вынесены в `src/i18n/ru.json`, ни одной русской строки в компонентах
- Слой `platform/` абстрагирует различия TG/веб: initData, тема, haptic
- OpenAPI-клиент: типы генерируются `openapi-typescript` из
  `/api/openapi.json`, ручных типов нет
- Экраны:
  - `/login` — вход/регистрация с двумя вкладками
  - `/entry` — TelegramGate: initData → JWT, редирект по флагу
  - `/onboarding` — 4 шага (согласие + 3 вопроса) с точками прогресса
  - `/` — главная: кольцо цикла, дата ожидаемой менструации, кнопка
    «Отметить начало» (диалог: сегодня/вчера/выбрать дату)
  - `/calendar` — месяц с фазами (заливка/пунктир/точка), тап → лист
    логирования; свайп между месяцами (кнопки)
  - `/stats` — три числа (средняя длина, длительность, σ) + мини-гистограмма
  - `/settings` — тема (авто/светлая/тёмная), группы уведомлений и приватности
- `LogSheet` — нижний лист: интенсивность, настроение, симптомы, заметка ≤500
- `BottomNav` — 4 вкладки, иконки — абстрактные SVG (без гендерной символики)
- Дополнительно:
  - Автоматическое восстановление сессии из refresh-токена при возврате
  - CalendarPage: чинит `Intl` (убирает «г.», ручной capitalize)
  - Кольцо анимируется от 0% до текущей доли за 900 мс при входе
  - Пунктирная обводка для прогноза (различимость не только по цвету, 2.1 ТЗ)
  - Токен `--on-accent` подбирает контрастный цвет текста на кнопке для
    обеих тем (WCAG AA)
- Скриншоты сохранены в `docs/screens/` (light + dark для всех экранов)

### Метрики сборки и приёмки

| Метрика | Значение | Порог | Статус |
|---|---|---|---|
| Bundle JS (min) | 240.14 KB | — | — |
| Bundle JS (gzip) | **76.31 KB** | < 250 KB | ✅ |
| Bundle CSS (gzip) | 4.13 KB | — | ✅ |
| Lighthouse Accessibility | **100** | > 90 | ✅ |
| Lighthouse Best Practices | **100** | — | ✅ |
| Lighthouse SEO | 91 | — | — |
| LCP (mobile emulation) | **204 ms** | < 2500 ms | ✅ |
| CLS | **0.00** | < 0.1 | ✅ |

Bundle 76 КБ gzip — в 3+ раза меньше бюджета NFR-3. Без сторонних UI-либ,
без Framer Motion, без иконочного шрифта.

### Дизайн
- Палитра «Тёплый песок» утверждена, значения в OKLCH
- MCP настроены в `.mcp.json`: chrome-devtools, playwright, figma

## Следующий шаг

Спринт 5 — уведомления. Планировщик, дедупликация через UNIQUE в
notifications, Telegram-канал, Web Push (VAPID + service worker), инструкция
установки PWA на iOS. Промпт в `CLAUDE.md`, раздел 15.

## Где остановились

—
